# MAD2 — Modern Applicat

*34 questions · transcribed from official practice paper (text layer + vision-checked)*

Legend: **Accepted answer** = the paper's own key (SA numerics). MCQ/MSQ keys are NOT printed in papers — answers: see the kit's RATTA / MCQ-MASTER files.

## Q1 · `6406532128287` · MCQ · 2.0 marks

Which of the following statements is true regarding the "this" keyword in JavaScript?

**Options:**
- "this" in an arrow function refers to the global object.
- "this" inside a regular function refers to the object that owns the function.
- Arrow functions do not have their own "this" and inherit it from the surrounding scope.
- "this" always points to the window object in browser environments.

*Solved in practice render (unofficial, verify with kit): Arrow functions do not have their own "this" and inherit it from the surrounding scope*


## Q2 · `6406532128288` · MCQ · 2.0 marks

Consider the below JavaScript program. What will be the output of the above program?

**Options:**
- 0
- 3
- 6
- 9
- 12

**Code / figure:**

```
let arr = [1, 2, 3]; let sum = arr.reduce((acc, val, idx, array) => acc + val, 0); console.log(sum);
```

*Solved in practice render (unofficial, verify with kit): 6*


## Q3 · `6406532128292` · MCQ · 2.0 marks

What is the main advantage of using JWT over session-based authentication?

**Options:**
- JWTs require server-side storage for each session.
- JWTs are stateless and eliminate the need for server-side session storage.
- JWTs are longer and contain more data than session IDs.
- JWTs automatically handle token expiration without additional logic.


*Solved in practice render (unofficial, verify with kit): JWTs are stateless and eliminate the need for server-side session storage*

## Q4 · `6406532128294` · MCQ · 2.0 marks

Which of the following HTTP methods does WebSocket use for initial handshake?

**Options:**
- GET
- POST
- PUT
- DELETE


*Solved in practice render (unofficial, verify with kit): GET*

## Q5 · `6406532128307` · MCQ · 2.0 marks

You are building a cricket analytics app. The file battingStats.js contains the following exports: In another file app.js, which of the following import statements will correctly import both strikeRate and playerName ? ( Assume both files are in the same directory )

**Code / figure:**

```
export const strikeRate = (runs, balls) => { return (runs / balls) * 100; };
export const playerName = "Virat Kohli";
```

**Options:**

- import strikeRate, playerName from "./battingStats.js";
- import { strikeRate, playerName } from "./battingStats.js";
- import * as stats from "./battingStats.js";
- require("./battingStats.js");

*Solved in practice render (unofficial, verify with kit): import { strikeRate, playerName } from "./battingStats.js";*



**Code / figure:**

```
export const strikeRate = (runs, balls) => { return (runs / balls) * 100; };
export const playerName = "Virat Kohli";
```

*Solved in practice render (unofficial, verify with kit): import { strikeRate, playerName } from "./battingStats.js";*

## Q6 · `6406532128309` · MCQ · 2.0 marks

*[stem on image — see EXTRA below]*

**Options:**

- Encryption keys used to secure the token
- User-related data (claims) such as identity and expiration time
- The algorithm used for signing the token
- The hashed signature of the token

*Solved in practice render (unofficial, verify with kit): User-related data (claims) such as identity and expiration time*


## Q7 · `6406532128289` · MCQ · 3.0 marks

Consider the below JavaScript program. What will be the output of the above program?

**Options:**
- Success!
- Error!
- Success! Error!
- Error! Success!

**Code / figure:**

```
const promise = new Promise((resolve, reject) => { resolve('Success!'); reject('Error!'); });
promise.then(console.log).catch(console.log);
```

*Solved in practice render (unofficial, verify with kit): Success!*


## Q8 · `6406532128290` · MCQ · 3.0 marks

Consider the below Vuex store configuration:

**Code / figure:**

```
const store = new Vuex.Store({ state:{ items:['Angular','Vue'] },
mutations:{ addItem(state, item){ state.items.push(item); } },
actions:{ addItemAsync({ commit }, item){ setTimeout(() => { commit('addItem', item); }, 500); } } });
```

**Options:**

- ['Angular','Vue']
- ['Angular','Vue','React']
- ['React']
- ['Angular']

*Solved in practice render (unofficial, verify with kit): ['Angular','Vue','React']*



**Code / figure:**

```
const store = new Vuex.Store({ state:{ items:['Angular','Vue'] },
mutations:{ addItem(state, item){ state.items.push(item); } },
actions:{ addItemAsync({ commit }, item){ setTimeout(() => { commit('addItem', item); }, 500); } } });
```

*Solved in practice render (unofficial, verify with kit): ['Angular','Vue','React']*

## Q9 · `6406532128291` · MCQ · 3.0 marks

Consider the below Vue app. What happens when the user navigates to an undefined route like "/random"?

**Code / figure:**

```
routes: [{ path:'/home', component:Home }, { path:'/about', component:About }, { path:'*', redirect:'/about' }]
```

**Options:**

- Browser navigates to /random and shows a blank page
- Browser navigates to /random but renders nothing
- Browser redirects to /about and displays "About"
- Error: No matching route

*Solved in practice render (unofficial, verify with kit): Browser redirects to /about and displays "About"*



**Code / figure:**

```
routes: [{ path:'/home', component:Home }, { path:'/about', component:About }, { path:'*', redirect:'/about' }]
```

*Solved in practice render (unofficial, verify with kit): Browser redirects to /about and displays "About"*

## Q10 · `6406532128296` · MCQ · 3.0 marks

What will be the output of the following JavaScript code?

**Options:**
- number, object, false, false
- NaN, null, true, true
- number, object, true, false
- NaN, null, false, false

**Code / figure:**

```
console.log(typeof NaN); console.log(typeof null); console.log(NaN == null); console.log(NaN === null);
```

*Solved in practice render (unofficial, verify with kit): number, object, false, false*


## Q11 · `6406532128297` · MCQ · 3.0 marks

Consider the following JavaScript program:

*Note: question body was on a missing render (p007); stem/options from text layer above if present.*




## Q12 · `6406532128298` · MCQ · 3.0 marks

Consider the following JavaScript program: What will be the output of the above program?

**Code / figure:**

```
const person = { firstName:'John', lastName:'Doe', age:30, city:'New York' };
const { firstName, city, ...rest } = person;
const newPerson = { firstName, location:city, ...rest };
console.log(firstName); console.log(rest); console.log(newPerson);
```

**Options:**

- John { city:'New York', lastName:'Doe', age:30 } { firstName:'John', location:'New York', city:'New York', lastName:'Doe', age:30 }
- John { lastName:'Doe', age:30 } { firstName:'John', location:'New York', lastName:'Doe', age:30 }
- John { lastName:'Doe', age:30 } { firstName:'John', location:'New York', city:'New York', lastName:'Doe', age:30 }
- undefined { firstName:'John', city:'New York', lastName:'Doe', age:30 } { firstName:'John', location:'New York' }

*Solved in practice render (unofficial, verify with kit): John { lastName: 'Doe', age: 30 } { firstName: 'John', location: 'New York', lastName: 'Doe', age: 30 }*



**Code / figure:**

```
const person = { firstName:'John', lastName:'Doe', age:30, city:'New York' };
const { firstName, city, ...rest } = person;
const newPerson = { firstName, location:city, ...rest };
console.log(firstName); console.log(rest); console.log(newPerson);
```

*Solved in practice render (unofficial, verify with kit): John { lastName: 'Doe', age: 30 } { firstName: 'John', location: 'New York', lastName: 'Doe', age: 30 }*

## Q13 · `6406532128299` · MCQ · 3.0 marks

What happens when this Flask caching code runs?

**Code / figure:**

```
(flask_caching SimpleCache: @cache.memoize(timeout=300) compute_data sleeps 5s; requests for user IDs 1, 2, 1 within 5 min)
```

**Options:**

- 15 seconds
- 10 seconds
- 5 seconds
- 0 seconds

*Solved in practice render (unofficial, verify with kit): 10 seconds*



**Code / figure:**

```
(flask_caching SimpleCache: @cache.memoize(timeout=300) compute_data sleeps 5s; requests for user IDs 1, 2, 1 within 5 min)
```

*Solved in practice render (unofficial, verify with kit): 10 seconds*

## Q14 · `6406532128306` · MCQ · 3.0 marks

Consider the following JavaScript program:

**Code / figure:**

```
const CAPACITY = 75; const TOTAL_PEOPLE = 213;
function allocatePeople(){ return new Promise((resolve, reject) => { let slots = [0,0,0]; let remaining = TOTAL_PEOPLE;
for (let i = 0; i < slots.length; i++) { if (remaining >= CAPACITY) { slots[i] = CAPACITY; remaining -= CAPACITY; } else { slots[i] = remaining; remaining = 0; } }
reject(slots); }); }
async function startAllocation(){ try{ const result = await allocatePeople(); console.log("Passed " + JSON.stringify(result)); }
catch(error){ console.log("Failed " + JSON.stringify(error)); } }
startAllocation();
```

**Options:**

- Passed [75,75,63]
- Failed [0,0,0]
- Failed [75,75,63]
- Passed [0,0,0]

*Solved in practice render (unofficial, verify with kit): Failed [75,75,63]*



**Code / figure:**

```
const CAPACITY = 75; const TOTAL_PEOPLE = 213;
function allocatePeople(){ return new Promise((resolve, reject) => { let slots = [0,0,0]; let remaining = TOTAL_PEOPLE;
for (let i = 0; i < slots.length; i++) { if (remaining >= CAPACITY) { slots[i] = CAPACITY; remaining -= CAPACITY; } else { slots[i] = remaining; remaining = 0; } }
reject(slots); }); }
async function startAllocation(){ try{ const result = await allocatePeople(); console.log("Passed " + JSON.stringify(result)); }
catch(error){ console.log("Failed " + JSON.stringify(error)); } }
startAllocation();
```

*Solved in practice render (unofficial, verify with kit): Failed [75,75,63]*

## Q15 · `6406532128312` · MCQ · 3.0 marks

Match the Vue directive in Directive with its correct functionality in Description using Vue.js.

**Code / figure:**

```
Match: 1.v-if→? 2.v-for→? 3.v-bind→? 4.v-model→? with A.two-way binding | B.conditional render | C.bind attributes | D.render list
```

**Options:**

- 1-A, 2-D, 3-C, 4-B
- 1-D, 2-B, 3-C, 4-A
- 1-B, 2-C, 3-D, 4-A
- 1-B, 2-D, 3-C, 4-A

*Solved in practice render (unofficial, verify with kit): 1-B, 2-D, 3-C, 4-A*



**Code / figure:**

```
Match: 1.v-if→? 2.v-for→? 3.v-bind→? 4.v-model→? with A.two-way binding | B.conditional render | C.bind attributes | D.render list
```

*Solved in practice render (unofficial, verify with kit): 1-B, 2-D, 3-C, 4-A*

## Q16 · `6406532128293` · MSQ · 2.0 marks

In a Single Page Application (SPA), what are the benefits of client-side routing over server-side routing?

**Options:**
- Slower navigation between pages
- Reduced load on the backend server
- Better SEO performance by default
- Seamless page transitions without full reloads

## Q17 · `6406532128320` · MSQ · 2.0 marks

Which of the following is the correct statement?

**Options:**
- Cookie size can impact the website performance.
- Cookie size does not affect performance in any practical/meaningful sense.
- A request to static.example.com will include cookies set for example.com.
- A request to static.example.com will not include cookies set for example.com.

## Q18 · `6406532128295` · MCQ · 4.5 marks

Consider the following Vue application with markup index.html and JavaScript file app.js.

**Code / figure:**

```
new Vue({ el:'#app', template:'<div>Status: {{status}}<br>Value: {{value}}</div>', data:{ status:"Initial", value:10 },
beforeCreate(){ this.status = this.status + " -> Created"; this.value = this.value + 5 },
beforeMount(){ this.status = this.status + " -> Ready"; this.value = this.value - 2 },
created(){ this.status = this.status + " -> Mounted"; this.value = this.value * 2 },
mounted(){ this.status = this.status + " -> Done"; this.value = this.value / 2 } })
```

**Options:**

- Status: Initial → Created → Ready → Mounted → Done Value: 13
- Status: Initial → Ready → Mounted → Done Value: 8
- Status: Initial → Created → Mounted → Ready → Done Value: 14
- Status: Initial → Mounted → Ready → Done Value: 9

*Solved in practice render (unofficial, verify with kit): Status: Initial → Mounted → Ready → Done Value: 9*

*Note: hook ORDER in output follows data-mutation sequence quirk; solved value as shown.*



**Code / figure:**

```
new Vue({ el:'#app', template:'<div>Status: {{status}}<br>Value: {{value}}</div>', data:{ status:"Initial", value:10 },
beforeCreate(){ this.status = this.status + " -> Created"; this.value = this.value + 5 },
beforeMount(){ this.status = this.status + " -> Ready"; this.value = this.value - 2 },
created(){ this.status = this.status + " -> Mounted"; this.value = this.value * 2 },
mounted(){ this.status = this.status + " -> Done"; this.value = this.value / 2 } })
```

*Solved in practice render (unofficial, verify with kit): Status: Initial → Mounted → Ready → Done Value: 9*

*Note: hook ORDER in output follows data-mutation sequence quirk; solved value as shown.*

## Q19 · `6406532128300` · MCQ · 4.5 marks

Consider the following endpoints created with a working setup of flask-security and flask-sqlalchemy.

**Code / figure:**

```
(@app.route('/api/open') open; @auth_required('token') /api/secure; @auth_required + @roles_required('superuser') /api/superuser)
Requests: GET /api/open (no auth) → ?; GET /api/secure (no auth) → ?; GET /api/superuser (valid token, role 'basic') → ?
```

**Options:**

- 200, 200, 200
- 200, 401, 403
- 200, 403, 401
- 401, 403, 403

*Solved in practice render (unofficial, verify with kit): 200, 401, 403*



**Code / figure:**

```
(@app.route('/api/open') open; @auth_required('token') /api/secure; @auth_required + @roles_required('superuser') /api/superuser)
Requests: GET /api/open (no auth) → ?; GET /api/secure (no auth) → ?; GET /api/superuser (valid token, role 'basic') → ?
```

*Solved in practice render (unofficial, verify with kit): 200, 401, 403*

## Q20 · `6406532128301` · MCQ · 4.5 marks

Consider the following code snippet running in a browser. What will be the output on the console?

**Options:**
- Ambrane 20 50 30
- Polycab 50 50 30
- Ambrane 50 50 30

**Code / figure:**

```
function inventory(){ const item = { name:"Polycab", stats:{ power:20 } };
const ref1 = item; const ref2 = { ...item }; const ref3 = { name:item.name, stats:{ ...item.stats } };
ref1.name = "Ambrane"; ref2.stats.power = 50; ref3.stats.power = 30;
console.log(item.name); console.log(item.stats.power); console.log(ref2.stats.power); console.log(ref3.stats.power); }
inventory();
```

*Solved in practice render (unofficial, verify with kit): Ambrane 50 50 30*


## Q21 · `6406532128302` · MCQ · 4.5 marks

Consider the following code snippet running on a browser. What is the output on the console?

**Options:**
- 100 100 100
- 100 undefined 100
- undefined undefined undefined
- 100 undefined undefined

**Code / figure:**

```
const obj = { value:100, outerFunc:function(){ const innerFunc = () => { console.log(this.value); }; return innerFunc; },
otherFunc:function(){ console.log(this.value); } };
const func1 = obj.outerFunc(); const func2 = obj.outerFunc; const func3 = obj.otherFunc;
func1(); func2()(); func3();
```

*Solved in practice render (unofficial, verify with kit): 100 undefined undefined*


## Q22 · `6406532128303` · MCQ · 4.5 marks

Consider the flask application app.py and an HTML file index.html:

*Note: question body was on a missing render (p015); only partial options (50, 50) visible.*




## Q23 · `6406532128305` · MCQ · 4.5 marks

A student is going on a trip and wants to check whether all essential items are packed. The application uses Vue 2 and Vuex to manage the list of packed items.The following Vue application is incomplete. Two lines of code are missing and are marked as Line X and Line Y. Which of the following options correctly fills Line X and Line Y so that the application works as expected?

**Code / figure:**

```
(Vuex store: state.packed [], ADD_ITEM mutation; component computed Line X, methods packItem Line Y)
```

**Options:**

- Line X packedItems() { return this.store.state.packed; } / Line Y this.store.commit
- Line X packedItems() { return this.$store.state.packed; } / Line Y this.$store.commit
- Line X packedItems() { return this.$store.packed; } / Line Y this.commit
- Line X packedItems: this.$store.state.packed / Line Y dispatch

*Solved in practice render (unofficial, verify with kit): Line X packedItems() { return this.$store.state.packed; } / Line Y this.$store.commit("ADD_ITEM", item)*



**Code / figure:**

```
(Vuex store: state.packed [], ADD_ITEM mutation; component computed Line X, methods packItem Line Y)
```

*Solved in practice render (unofficial, verify with kit): Line X packedItems() { return this.$store.state.packed; } / Line Y this.$store.commit("ADD_ITEM", item)*

## Q24 · `6406532128304` · MSQ · 3.0 marks

Which of the following statement(s) is/are true about GraphQL?

**Options:**
- Mutations in GraphQL can be used to change the underlying data store.
- GraphQL services are created by defining types and fields of those types, then providing functions for each field.
- GraphQL can only fetch data from a single source.
- GraphQL allows only GET operations.

## Q25 · `6406532128308` · MSQ · 3.0 marks

Consider the following code:

**Code / figure:**

```
(Flask + Flask-JWT-Extended: POST /login {username:Jassi} → token; GET /stats @jwt_required returns Welcome user)
```

**Options:**

- /stats returns "Welcome Jassi" only with valid JWT in Authorization header
- No token → unauthorized error
- Token must be sent as JSON in body
- None of these

*Solved in practice render (unofficial, verify with kit): Valid-JWT-header option || No-token-unauthorized option*



**Code / figure:**

```
(Flask + Flask-JWT-Extended: POST /login {username:Jassi} → token; GET /stats @jwt_required returns Welcome user)
```

*Solved in practice render (unofficial, verify with kit): Valid-JWT-header option || No-token-unauthorized option*

## Q26 · `6406532128310` · MSQ · 3.0 marks

Hamza is working on a project using Git and performs the following operations: Creates a new branch and switches to it1. Stages changes for commit2. Commits changes with a message3. Pushes the branch to a remote repository4. Which of the following commands are correctly used for these operations?

**Options:**

- git checkout -b feature-branch
- git add .
- git commit -m "Initial commit"
- git push origin feature-branch
- git branch feature-branch && git checkout main
- git push feature-branch origin

*Solved in practice render (unofficial, verify with kit): git checkout -b feature-branch || git add . || git commit -m "Initial commit" || git push origin feature-branch*


## Q27 · `6406532128311` · MSQ · 3.0 marks

Prerna and Swathy are using a web application built with Vue.js for the BS Degree program. When the page loads, the sidebar content appears instantly• However, when they click on an Assignments link, the main content takes noticeable time to load • A simplified version of the component is shown below: Which of the following statements correctly explain why the Assignments page takes time to load?

**Options:**
- The sidebar is likely part of a parent component and is already rendered before the API call
- The Assignments component waits for the API response before displaying data
- Vue blocks rendering of all components until API calls complete
- The delay is caused because data is fetched asynchronously after the component is created

**Code / figure:**

```
(Assignments component fetches /api/assignments in created(); sidebar instant, main slow)
```

*Solved in practice render (unofficial, verify with kit): sidebar-parent option || waits-for-API option || async-fetch option*


## Q28 · `6406532128313` · COMPREHENSION · 0.0 marks

Consider the following Vue.js application:

**Code / figure:**

```
new Vue({ el:"#app", data:{ a:1, b:2, time:0 },
computed:{ bigNumber(){ console.log("computed run"); let s = 0; for (let i = 0; i < 10000000; i++) s += i; return s + this.a + this.b; } },
methods:{ calculate(){ this.time = Date.now(); } } })
```


## Q29 · `6406532128314` · MCQ · 3.0 marks

How many times will "computed run" be logged in the console during the initial render?

**Options:**
- 0
- 1
- Multiple times due to the loop
- Depends on user interaction


*Solved in practice render (unofficial, verify with kit): 1*

## Q30 · `6406532128315` · MCQ · 3.0 marks

If the method is modified as follows: What will happen when the button is clicked?

**Code / figure:**

```
calculate() { this.a = this.a + 1; }
```

**Options:**

- bigNumber() will not run again
- bigNumber() will run again because a dependency changed
- Only time will update
- Vue will throw an error

*Solved in practice render (unofficial, verify with kit): bigNumber() will run again because a dependency changed*



**Code / figure:**

```
calculate() { this.a = this.a + 1; }
```

*Solved in practice render (unofficial, verify with kit): bigNumber() will run again because a dependency changed*

## Q31 · `6406532128316` · MCQ · 3.0 marks

Why is defined as a computed property instead of a method?

**Options:**
- Computed properties are always faster than methods
- Methods cannot access data properties
- Computed properties are cached based on their dependencies
- Computed properties run only once


*Solved in practice render (unofficial, verify with kit): Computed properties are cached based on their dependencies*

## Q32 · `6406532128317` · COMPREHENSION · 0.0 marks

Consider the following Vue 2 application:

**Code / figure:**

```
const Item = { template:'<p>Item ID from route: {{ $route.params.id }}</p>', updated(){ console.log("updated hook called"); } };
routes: [{ path:"/item/:id", component:Item }]; watch: { "$route.params.id"(newId){ this.currentId = newId; } }
Load #/item/1, click Item 2 link.
```


## Q33 · `6406532128318` · MCQ · 4.5 marks

What happens immediately after clicking "Item 2"?

**Options:**
- The Item component is destroyed and recreated, so
- is never called
- The route changes, the component is reused, and
- is called
- The route changes, but neither watcher nor lifecycle hooks run
- A new Vue instance is created for the route


*Solved in practice render (unofficial, verify with kit): The route changes, the component is reused, and updated() is called*

## Q34 · `6406532128319` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

*Note: question body was beyond rendered pages (p022+); stem/options from text layer above if present.*



