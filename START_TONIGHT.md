# 🌙 START_TONIGHT — Read This First (5 min)

**Exam: TOMORROW. You studied nothing. Fine. This kit is everything.**
Every question below comes from 27 real papers (11 BA + 12 MAD2 + 10 MLP, 2022→May 2026).
Nothing here is generic theory. Every line maps to marks.

## 🥇 THE #1 RULE (memorize this — it resolves every confusion)

> **If the exam question has the EXACT SAME NUMBERS as a PYQ in this kit → copy the PYQ's accepted answer from the VAULT box. Don't recompute.**
> **If ANY number is different → run the ENGINE steps. The engine always works.**

Why: 3 portal answer-keys look buggy vs correct math. Exact repeat = same (buggy) key = free marks if you copy. Changed numbers = fresh key = engine wins.

## ⏰ TONIGHT'S SCHEDULE (pick your hours)

**If you have ~9 hours:**
| Hour | Do | File |
|---|---|---|
| 0:00–0:20 | Read this + EXAM_DAY_OS universal algorithm | `00_EXAM_DAY_OS.md` |
| 0:20–2:20 | **BA engines** (biggest marks-per-page: DEA-HCU + regression + chi-square repeat endlessly) | `01_BA/` |
| 2:20–3:00 | BA ratta MCQ/MSQ + formula sheet | `01_BA/` |
| 3:00–5:00 | **MAD2 MCQ master + MSQ playbook** (pure memorization, 100 marks paper) | `02_MAD2/` |
| 5:00–5:30 | MAD2 JS/Vue/Web engines (only the trigger tables) | `02_MAD2/` |
| 5:30–7:30 | **MLP engines + rattas** (sklearn outputs are 60% of the paper) | `03_MLP/` |
| 7:30–8:30 | Re-read all 3 formula/cheat sheets once | `*-SHEET.md` |
| 8:30–9:00 | LAST_6_HOURS file (final pass) | `99_LAST_6_HOURS.md` |

**If you have only ~4 hours:** read `99_LAST_6_HOURS.md` — it IS the 4-hour plan. Follow it blindly.

**If you have only ~1 hour:** read the 3 `*-RATTA*` files' ⭐ HIGH sections + `99_LAST_6_HOURS.md`.

## 📁 READING ORDER (never read cover-to-cover — read in this order)

1. `00_EXAM_DAY_OS.md` — the blank-mind algorithm. THE most important file.
2. Your weakest subject's ENGINE files (mechanical steps).
3. Your weakest subject's RATTA file (memorize answers).
4. Next subject. Next subject.
5. All `*SHEET.md` files right before sleeping + once before exam.

## ⚠️ 5 EXAM-HALL FACTS (save you 10 marks of silly loss)

1. **Q1 of every paper is worth 0 marks** — "Are you sure this is your subject? YES/NO". Click YES. Don't sweat it.
2. **SA (numeric) answers**: type ONLY the number. No commas (write `2968750` not `29,68,750`). No % symbol when they say "without the percentage symbol". Read the note under EVERY SA question — it tells rounding (`[R2]` = 2 decimals).
3. **Rounding**: "rounded to two decimal places" = normal rounding (2.675 → 2.68). "Without rounding off / truncated" (MLP 2024 paper instruction) = CUT digits (2.679 → 2.67). The note decides — always obey the note.
4. **MSQ**: you must tick ALL correct options. Partial marking is rare — a wrong tick can zero the question. The MSQ-PLAYBOOK tells you how many to tick per pattern.
5. **Comprehension (0-mark) parents**: they carry the DATA TABLE. Read the table, not the story. The story is always fake ("purely hypothetical"). Only numbers matter.

## 📊 WHERE THE MARKS ARE (80/20)

- **BA (45 marks, ~40 Qs):** ~20 marks = SA numerics (DEA-HCU, regression/elasticity, chi-square, Bayes, confusion metrics). ~15 marks = MCQ/MSQ repeats (DEA theory, conjoint counts, productive efficiency). ~10 marks = scenario reading.
- **MAD2 (100 marks, ~35 Qs):** ~70 marks = MCQ (JS output 3m + 4.5m questions DECIDE your grade — the JS-ENGINE cracks all). ~30 marks = MSQ (MSQ-PLAYBOOK cracks most).
- **MLP (100 marks, ~35-40 Qs):** ~40 marks = "predict the code output" (MLP engines = run-the-mental-model recipes). ~30 marks = concept MCQ (same 40 questions repeat for 3 years). ~30 marks = MSQ + SA numerics.

Go. Start with `00_EXAM_DAY_OS.md`. No more planning — execute.
