# MAD2 — 2024 Dec22  IIT

*33 questions · transcribed from official practice paper (text layer + vision-checked)*

Legend: **Accepted answer** = the paper's own key (SA numerics). MCQ/MSQ keys are NOT printed in papers — answers: see the kit's RATTA / MCQ-MASTER files.

## Q1 · `6406531045091` · MCQ · 0.0 marks

THIS IS QUESTION PAPER FOR THE SUBJECT "DIPLOMA LEVEL: MODERN APPLICATION DEVELOPMENT 2 (COMPUTER BASED EXAM)". ARE YOU SURE YOU HAVE TO WRITE EXAM FOR THIS SUBJECT?

**Options:**

- YES

- NO

*Answer: YES (0-mark confirmation — click YES and move on).*

## Q2 · `6406531045092` · MCQ · 3.0 marks

Which of the following statements is true about closures in JavaScript?

**Options:**
- A closure can access variables from its outer function even after the outerfunction has returned.
- A closure can be created when a function is defined inside another function.
- Closures are useful for data encapsulation and controlling access to privatedata.
- All of these.


## Q3 · `6406531045093` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
console.log("Start"); setTimeout(function(){ console.log("Inside Timeout"); }, 0)
Promise.resolve().then(function(){ console.log("Inside Promise"); }); console.log("End");
```

**Options:**

- StartInside Timeout Inside Promise End
- StartInside Promise Inside Timeout End
- StartEnd Inside Timeout Inside Promise
- StartEnd Inside Promise Inside Timeout


## Q4 · `6406531045094` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**
- Hello, JohnTypeError: john.greet is not a function
- Hello, JohnHello, undefined
- TypeError: john.greet is not a functionTypeError: john.greet is not a function
- Hello, JohnHello, John


**Code / figure:**

```
function Person(name){ this.name = name; }
Person.prototype.greet = function(){ console.log("Hello, " + this.name); };
const john = new Person("John"); john.greet(); delete john.greet; john.greet();
```

## Q5 · `6406531045101` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
new Vue({ el:'#app', data:{ user:{ name:'Abhi', age:30 } },
watch:{ user:{ handler(newValue, oldValue){ console.log('User object changed:', newValue); }, deep:true } } })
Execute: app.user.name = "Dev"
```

**Options:**

- The watcher will not be triggered (nested property)
- The watcher will be triggered and log the changed user object
- The watcher will trigger but log only the new name value
- The watcher will throw an error (deep watching unsupported)


## Q6 · `6406531045103` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
<template><div><p v-if="isVisible">This paragraph is visible</p><p v-else>This paragraph is hidden</p></div></template>
data: isVisible:true. Execute: this.isVisible = false
```

**Options:**

- The "visible" paragraph will be displayed
- The "hidden" paragraph will be displayed
- Both paragraphs will be displayed
- No change will happen


## Q7 · `6406531045105` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**
- secure=True and samesite='Strict'
- secure=False and samesite='Strict'
- secure=True and samesite='None'
- secure=False and samesite='Lax'


**Code / figure:**

```
response.set_cookie('session_id', 'abc123', secure=option1, samesite=option2)
```

## Q8 · `6406531045106` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
function addItemToCart(item){ let cart = JSON.parse(localStorage.getItem('cart')) || []; cart.push(item); localStorage.setItem('cart', JSON.stringify(cart)); }
function getCartItems(){ return JSON.parse(localStorage.getItem('cart')) || []; }
addItemToCart({ id:1, name:'Laptop' }.name); console.log(getCartItems());
(page loaded fresh, then refreshed twice = 3 runs)
```

**Options:**

- []
- ['Laptop', 'Laptop', 'Laptop']
- [{ id:1, name:'Laptop' }]
- [undefined]


## Q9 · `6406531045107` · MCQ · 3.0 marks

Which of the following is true regarding long polling?

**Options:**
- Long polling opens multiple connections between the client and the server,and the server continuously sends updates to
- the client in real-time.
- Long polling involves the client repeatedly sending requests at fixed intervalsto the server to check for updates,
- regardless of whether new data is available.
- Long polling allows the client to make a single request to the server, where theserver holds the connection open until new
- data is available and then sends the response.
- Long polling uses WebSockets to maintain a persistent, bidirectionalconnection between the client and the server.

## Q10 · `6406531045109` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**

- All of these

*Note: question body was on a missing render page; only tail option visible.*




## Q11 · `6406531045110` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**
- No authentication check
- SQL injection vulnerability
- Unvalidated user input

## Q12 · `6406531045111` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
Vue.component('child', { template:`<div><slot name="header" :info="info"></slot><slot :info="info"></slot></div>`,
data(){ return { info:{ title:'Hello', desc:'World' } } } })
```

**Options:**

- <template v-slot:header="data">{{data.title}}</template>
- <template v-slot:header="{ info }">{{info.title}}</template>
- <template v-slot:header>{{info.title}}</template>
- <template v-slot:header=info>{{info.title}}</template>


## Q13 · `6406531045112` · MCQ · 3.0 marks

What will be the output on the console?

**Code / figure:**

```
Promise.resolve(1).then(x => x + 1).then(x => Promise.resolve(x + 1)).then(x => { throw 'error' }).catch(e => e + 4).then(x => console.log(x))
```

**Options:**

- error4
- 7
- error
- undefined


## Q14 · `6406531045114` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**
- Begin, Start, 5, End, Finish
- Begin, Finish, Start, 5, End
- Begin, Finish, Start, End, 5
- Begin, Start, Finish, 5, End


**Code / figure:**

```
async function test(){ console.log("Start"); const val = await Promise.resolve(5); console.log(val); return "End"; }
console.log("Begin"); test().then(data => console.log(data)); console.log("Finish");
```

## Q15 · `6406531045117` · MCQ · 3.0 marks

Which of the following best describes the difference between @cache.memoize() and @cache.cached() in Flask-Caching?

**Options:**

- @cache.memoize() caches function results based on arguments, while @cache.cached() caches entire views
- @cache.cached() caches by arguments, while @cache.memoize() caches entire views
- Both cache entire views, memoize uses Redis by default
- Both identical, different syntax


## Q16 · `6406531045119` · MCQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Options:**
- It will display the indices, values and types of all the items of the array.
- It will throw error on the console for the very first iteration.
- It will display the index, value and type of the value for the first item of arrayand then will throw error for the next value.
- None of these.


**Code / figure:**

```
let x = [100, 'x', num => num + 1]; for (const i = 0; i<x.length; i++) { console.log(i, x[i], typeof(x[i])) }
```

## Q17 · `6406531045095` · MCQ · 2.0 marks

Which of the following statements is false when using the “async” and “await” keywords in JavaScript?

**Options:**
- The “await” keyword can only be used inside an async function.
- The “await” keyword pauses the execution of the surrounding async functionuntil the promise resolves.
- The async functions always return a promise, even if the return value is not apromise.
- The async functions run synchronously, but their await statements executeasynchronously.



## Q18 · `6406531045096` · MCQ · 2.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
const obj = { name:"Abhi", greet:function(){ console.log(this.name); } }; const greet = obj.greet; greet();
```

**Options:**

- Abhi
- undefined
- null
- Reference Error
- Garbage Value


## Q19 · `6406531045098` · MCQ · 2.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
const person = { firstName:"John", lastName:"Doe", fullName:function(){ return this.firstName + " " + this.lastName; } }
const newPerson = person.fullName.bind({ firstName:"Jane", lastName:"Smith" }); console.log(newPerson());
```

**Options:**

- John Doe
- Jane Smith
- John Smith
- Jane Doe
- undefined
- null
- Reference Error


## Q20 · `6406531045102` · MCQ · 2.0 marks

Which of the following statements about the beforeDestroy and destroyed lifecycle hooks in Vue is true?

**Options:**
- The “beforeDestroy” hook is called after the component is removed from theDOM, and the “destroyed” hook is called
- before the component is destroyed.
- The “beforeDestroy” hook is called before the component is removed from theDOM, and the “destroyed” hook is called
- after the component is destroyed.
- The “beforeDestroy” hook is called when the component is mounted, and the“destroyed” hook is called before the
- component is destroyed.
- Both hooks are called after the component is destroyed.

## Q21 · `6406531045104` · MCQ · 2.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
function add(x){ return function(y){ return x + y; }; }; const addFive = add(5); console.log(addFive(10));
```

**Options:**

- 5
- 10
- 15
- 20


## Q22 · `6406531045113` · MCQ · 2.0 marks

Which of the following statements is FALSE?

**Options:**
- Cache-Control: no-store prevents caching
- ETag helps validate cache freshness
- Data stored in sessionStorage remains available even after the browser isclosed and reopened.
- localStorage has larger storage limit than cookies

## Q23 · `6406531045115` · MCQ · 2.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
CSP Header: Content-Security-Policy: default-src 'self'; HTML: <script src="https://otherurl.com/code.js"></script>
```

**Options:**

- The script from otherurl.com will load normally
- The script will fail to load, and an error will be logged in the console
- The browser will display a warning and allow the script to load
- Nothing will happen; the CSP only affects inline scripts


## Q24 · `6406531045097` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
(grandparent > parent > child divs; ALL THREE listeners with capture:true logging GrandParent/Parent/Child; click "Child")
```

**Options:**

- ChildParent GrandParent
- Child
- GrandParentParent Child
- None of these


## Q25 · `6406531045099` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
const store = new Vuex.Store({ state:{ counter:0 }, mutations:{ increment(state){ state.counter++; } },
actions:{ async incrementAsync({ commit }){ await new Promise(resolve => setTimeout(resolve, 1000)); commit('increment'); } } })
Execute: this.$store.dispatch('incrementAsync');
```

**Options:**

- The increment mutation will be called immediately after the dispatch
- The state will be updated after the asynchronous operation completes
- The action will execute synchronously, committing before the promise resolves
- The action will be skipped since mutations cannot be called inside actions


## Q26 · `6406531045108` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
Vue.component('my-button', { template:`<button @click="handleClick"><slot></slot></button>`,
methods:{ handleClick(){ console.log('Button'); this.$emit('click') } } })
new Vue({ el:'#app', template:`<my-button @click="parentClick">Click me</my-button>`, methods:{ parentClick(){ console.log('Parent') } } })
Button pressed once.
```

**Options:**

- Parent
- Button
- ButtonParent
- ParentButton


## Q27 · `6406531045116` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Options:**

- User: null
- The error will be logged, Nothing will be displayed
- User: Luke

*Note: question body was on a missing render page; only options visible.*


## Q28 · `6406531045118` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Options:**
- The userData in the store is set to the data from the API.


*Options image not recovered — check the kit's RATTA / MCQ-MASTER files for this question family.*

## Q29 · `6406531045121` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
let bsCourses = { subject:'MAD II', stream:'Programming' }; let esCourses = bsCourses; let msCourses = {};
for (let course in bsCourses){ msCourses[key] = bsCourses[key]; }
esCourses.subject = 'Embedded C'; msCourses.stream = 'Electronics'; console.log(msCourses)
```

**Options:**

- { subject:'MAD II', stream:'Programming' }
- { subject:'Embedded C', stream:'Electronics' }
- { subject:'MAD II', stream:'Electronics' }
- { subject:'Embedded C', stream:'Programming' }

*Note: loop uses undefined variable `key` (ReferenceError in strict reading); options as printed.*



**Code / figure:**

```
let bsCourses = { subject:'MAD II', stream:'Programming' }; let esCourses = bsCourses; let msCourses = {};
for (let course in bsCourses){ msCourses[key] = bsCourses[key]; }
esCourses.subject = 'Embedded C'; msCourses.stream = 'Electronics'; console.log(msCourses)
```

*Note: loop uses undefined variable `key` (ReferenceError in strict reading); options as printed.*

## Q30 · `6406531045122` · MCQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
var var1 = 56;
const exObj = { var2:24, var1:15, inObj:{ var1:75, inObjFunc:()=>{ return "Value is " + this.var1; } },
exObjFunc:function(){ let var2 = 14; return "Value is " + this.var2; } }
let x = exObj.inObj; console.log(x.inObjFunc()); console.log(exObj.exObjFunc())
```

**Options:**

- Value is 75 / Value is 24
- Value is 56 / Value is 24
- Value is 15 / Value is 14
- Value is 56 / Value is 14


## Q31 · `6406531045120` · MSQ · 3.0 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
(same getter/setter myBox object as paper-5 Q8 with tool/cutter)
```

**Options:**

- console.log(myBox.tools)
- console.log(myBox.tools())
- console.log(myBox.tools('ScrewDriver Grinder'))
- console.log(myBox.tools = 'ScrewDriver Grinder')


## Q32 · `6406531045123` · MSQ · 4.5 marks

*[stem on image — see EXTRA below]*

**Code / figure:**

```
(same v-if/v-show index1/index2 pair as paper-5 Q9)
```

**Options:**

- When the directive v-if is falsy, it removes the entire element from the DOM
- The raw HTML will be exactly the same when both values are set to true
- The raw HTML will be exactly the same for the current state of data
- When the directive v-show is falsy, it removes the entire element from the DOM


## Q33 · `6406531045100` · MSQ · 2.0 marks

*[stem on image — see EXTRA below]*

*Note: question content was blank in render; continuation page missing; unrecoverable.*



